# CLAUDE.md — 專案協作守則

這份檔案給 Claude Code 閱讀，用來在本研究專案中維持一致的工作方式。

## 專案定位

- 這是一個 **臨床預測研究**（pre-ESRD 腎病組合終點），也是 AI 課程的教學範例。
- 目標讀者是學生與研究者，**可重現性（reproducibility）與清楚的紀錄** 比執行速度更重要。
- 資料是**合成教學資料**，非真實病人；但仍以「真實研究」的嚴謹度來對待流程。

## 資料夾使用原則

- 依編號資料夾對應研究階段（見 `README.md`）。把檔案放到對的階段資料夾。
- `03_data/raw/` 是**唯讀原始資料**：絕不修改、不覆寫、不在此資料夾產生衍生檔。
  - 清理／衍生後的資料寫到 `03_data/interim/`（中間步驟）或 `03_data/processed/`（可直接分析）。
- 探索性分析放 `04_code/notebooks/`；穩定、可重用的函式抽到 `04_code/src/`。
- 每個建模實驗在 `06_experiments/` 開一個獨立子資料夾（如 `exp001_logistic_baseline/`），內含設定、程式、結果與 README。
- 圖表輸出到 `05_analysis/figures/`，表格到 `05_analysis/tables/`，統計結果到 `05_analysis/statistics/`。

## 研究日誌（重要）

- 每次有意義的進展，都在 `09_logs/research_log/` 新增或更新當日 log（檔名 `YYYY-MM-DD.md`）。
- 一則 log 至少包含：**做了什麼 / 為什麼 / 結果 / 下一步**。

### 決策紀錄規則（務必遵守）
- **凡是「重要決策」一律要在當日 log 留下：決策內容 + 理由 + 被否決的替代方案**。
- 下列情況都算重要決策，遇到就記，不要略過：
  1. 研究設計：改假說、改終點定義、納入／排除條件。
  2. 資料：遺漏值處理策略、變項轉換（如 log）、排除變項或個案、資料版本更替。
  3. 建模：換模型、特徵集變動、類別不平衡處理方式、超參數重大調整。
  4. 統計：檢定方法、顯著水準、多重比較校正的選擇。
  5. 工程／版控：分支與 PR 結構、預設分支變更、套件版本鎖定策略、環境設定。
  6. 對外部意見的取捨：審查建議（含 bot）採納或否決的理由。
- 若某決策尚未定案，於 log 標記為「待定」並寫下待決問題，定案後回填。

## 可重現性

- 分析盡量用**腳本**而非手動步驟；固定亂數種子（`random_state=42`）。
- 記錄套件版本（`requirements.txt`）。
- 檔案命名用日期或流水號，避免 `final_v2_really_final`。

## 統計與方法學

- 事件率約 13%，屬**不平衡類別**；報告時重視 AUROC、AUPRC、校準（calibration），不要只看 accuracy。
- 遺漏值（`bmi`, `upcr_mg_g`, `albumin`, `hba1c`）需明確描述處理方式（刪除／插補），並記錄理由。
- 做任何統計檢定前，先在 `02_planning/` 的分析計畫（SAP）寫下計畫，避免事後挑選。

## Git 習慣

- Commit 訊息清楚描述「這次改了什麼、屬於哪個研究階段」。
- 大型資料、模型權重、`.venv/`、輸出中間檔不進版控（見 `.gitignore`）。
- 原始資料 `03_data/raw/` 可入版控（此為小型合成資料）；真實研究請改用資料治理流程。

## 語言

- 與使用者溝通、文件與 log 以**繁體中文**為主；程式碼變數與註解可用英文。
